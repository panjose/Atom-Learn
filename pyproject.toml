[project]
name = "atom-learn"
version = "0.4.0"
description = "Persistent source-grounded learning, research, and exam-preparation Skill"
requires-python = ">=3.10"
dependencies = [
  "PyYAML>=6.0",
  "pypdf>=4.3",
  "python-docx>=1.1",
]

[tool.pytest.ini_options]
addopts = "-ra"
testpaths = ["tests"]
